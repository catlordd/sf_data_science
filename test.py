import numpy as np
'''def txt_s(text):
    st1 = text[0]
    i = 0
    tpl = ('', 0)
    max_tpl = ('', 0)
    for ii, sym in enumerate (text):
        if st1 == sym:
            i += 1
            tpl = (sym , i)
            if tpl[1] > max_tpl[1]:
                max_tpl = tpl
                
        else:
            st1 = sym
            i =1
    return max_tpl[1]
print(txt_s('asfdaafddddfeef'))'''
